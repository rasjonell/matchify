generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id            String   @id @default(cuid())
  created_at    DateTime
  access_token  String
  refresh_token String
  name          String
  spotify_url   String
}
